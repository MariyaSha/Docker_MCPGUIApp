# Add remote servers as a separate services
registry:
  huggingface:
    remote:
      url: "https://huggingface.co/mcp"
      transport_type: http
      # Hugging Face authentication is optional!
      # headers: 
      #   Authorization: "Bearer <HuggingFace API key goes here>"
  stripe-remote:
    remote:
      url: "https://mcp.stripe.com"
      transport_type: http
      headers: 
        Authorization: "Bearer {{STRIPE_SECRETS}}"

